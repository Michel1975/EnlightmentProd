<% provide(:title, 'Medlemslogin') %>
<% provide(:description, 'Login for medlemmer') %>
<% content_for :breadcrumps do %>
  <li class="active">Medlemslogin</li>
<% end %>
<div class="row-fluid">
  <div class="span12">
    <%= form_tag shared_member_sessions_path, class: "log-page" do %>    
      <h3>Login på din konto</h3>
      <div class="input-prepend">
        <span class="add-on"><i class="icon-user"></i></span>
        <%= text_field_tag :email, "", class: "input-xlarge", placeholder: "Email"%>    
      </div>
      <div class="input-prepend">
        <span class="add-on"><i class="icon-lock"></i></span>
        <%= password_field_tag :password, "", placeholder: "Password", class: "input-xlarge" %>    
      </div>
      <div class="controls form-inline">
        <%= check_box_tag :remember_me %>
        <%= label_tag :remember_me, "Husk loginoplysninger" %>
        <%= button_tag "Login", type: "submit", class: "submission btn-u btn-primary pull-right btn-u-blue" %>  
      </div>
      <hr />
      <h4>Glemt dit password ?</h4>
      <p>Klik <%= link_to "her", new_shared_password_reset_path, class: "color-green" %> for at nulstille dit password</p>
    <% end %>
  </div><!--/span12-->
</div><!--/row fluid-->

<% content_for :scripts do %>
    <script type="text/javascript">
        jQuery(document).ready(function() {
            App.init();
        });

        jQuery(function($) {
          $("form").on("submit", function () {
            $(this).find(".submission").prop("disabled", true);
          });
        });

        (function(d, s, id) {
            var js, fjs = d.getElementsByTagName(s)[0];
            if (d.getElementById(id)) return;
                js = d.createElement(s); js.id = id;
                js.src = "//connect.facebook.net/da_DK/all.js#xfbml=1&appId=155627067978522";
                fjs.parentNode.insertBefore(js, fjs);
        }
        (document, 'script', 'facebook-jssdk'));
    </script> 
<% end %>

  
  
